{# TrustMRR-style bottom-right subscription popup — slides up after 3s #}
<style>
  #sub-popup {
    position: fixed;
    bottom: 24px;
    right: 24px;
    width: 340px;
    z-index: 9999;
    transform: translateY(120%);
    transition: transform 0.5s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.4s ease;
    opacity: 0;
    pointer-events: none;
  }
  #sub-popup.popup-visible {
    transform: translateY(0);
    opacity: 1;
    pointer-events: all;
  }
  #sub-popup.popup-hidden {
    transform: translateY(120%);
    opacity: 0;
    pointer-events: none;
  }
  .sub-popup-card {
    background: #ffffff;
    border-radius: 18px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.18), 0 4px 16px rgba(0,0,0,0.08);
    overflow: hidden;
    border: 1px solid rgba(0,0,0,0.06);
  }
  .dark .sub-popup-card {
    background: #1a1f2e;
    border-color: rgba(255,255,255,0.08);
    box-shadow: 0 20px 60px rgba(0,0,0,0.5), 0 4px 16px rgba(0,0,0,0.3);
  }
  .sub-popup-top {
    height: 4px;
    background: linear-gradient(90deg, #6366f1, #8b5cf6, #a855f7);
  }
  .sub-popup-body {
    padding: 18px 20px 16px;
  }
  .sub-popup-header {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 10px;
  }
  .sub-popup-icon-wrap {
    width: 42px;
    height: 42px;
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    font-size: 20px;
  }
  .sub-popup-close {
    background: none;
    border: none;
    cursor: pointer;
    color: #9ca3af;
    padding: 2px;
    border-radius: 6px;
    line-height: 1;
    transition: color 0.2s, background 0.2s;
    flex-shrink: 0;
    margin-top: -2px;
  }
  .sub-popup-close:hover {
    color: #374151;
    background: #f3f4f6;
  }
  .dark .sub-popup-close:hover {
    color: #e5e7eb;
    background: rgba(255,255,255,0.1);
  }
  .sub-popup-title {
    font-size: 16px;
    font-weight: 800;
    color: #111827;
    margin: 0 0 4px;
    line-height: 1.3;
  }
  .dark .sub-popup-title {
    color: #f9fafb;
  }
  .sub-popup-desc {
    font-size: 13px;
    color: #6b7280;
    margin: 0 0 12px;
    line-height: 1.5;
  }
  .dark .sub-popup-desc {
    color: #9ca3af;
  }
  .sub-popup-input {
    width: 100%;
    padding: 10px 14px;
    border: 1.5px solid #e5e7eb;
    border-radius: 10px;
    font-size: 13.5px;
    color: #111827;
    background: #f9fafb;
    outline: none;
    box-sizing: border-box;
    transition: border-color 0.2s, box-shadow 0.2s;
    margin-bottom: 8px;
    font-family: inherit;
  }
  .dark .sub-popup-input {
    background: rgba(255,255,255,0.06);
    border-color: rgba(255,255,255,0.12);
    color: #f9fafb;
  }
  .sub-popup-input:focus {
    border-color: #6366f1;
    box-shadow: 0 0 0 3px rgba(99,102,241,0.12);
    background: #fff;
  }
  .dark .sub-popup-input:focus {
    background: rgba(255,255,255,0.08);
  }
  .sub-popup-input::placeholder {
    color: #9ca3af;
  }
  .sub-popup-btn {
    width: 100%;
    padding: 11px 16px;
    background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
    color: #fff;
    border: none;
    border-radius: 10px;
    font-size: 14px;
    font-weight: 700;
    cursor: pointer;
    transition: opacity 0.2s, transform 0.15s;
    font-family: inherit;
    letter-spacing: 0.01em;
  }
  .sub-popup-btn:hover {
    opacity: 0.92;
    transform: translateY(-1px);
  }
  .sub-popup-proof {
    font-size: 11.5px;
    color: #9ca3af;
    text-align: center;
    margin-top: 10px;
  }
  .dark .sub-popup-proof {
    color: #6b7280;
  }
  @media (max-width: 400px) {
    #sub-popup {
      width: calc(100vw - 32px);
      right: 16px;
      bottom: 16px;
    }
  }
</style>

<div id="sub-popup" role="dialog" aria-label="Subscribe to weekly visa jobs">
  <div class="sub-popup-card">
    <div class="sub-popup-top"></div>
    <div class="sub-popup-body">

      <div class="sub-popup-header">
        <div style="display:flex;align-items:center;gap:10px;flex:1;">
          <div class="sub-popup-icon-wrap">✈️</div>
          <div style="flex:1;">
            <p class="sub-popup-title">Get Visa Jobs in Your Inbox</p>
          </div>
        </div>
        <button id="sub-popup-close" class="sub-popup-close" aria-label="Close">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
        </button>
      </div>

      <p class="sub-popup-desc">Weekly curated jobs with visa sponsorship &amp; relocation — straight to your inbox. Zero spam.</p>

      <form action="{{ site.mailchimp.action }}" method="post" target="_blank" novalidate id="sub-popup-form">
        <input type="email" name="EMAIL" class="sub-popup-input" placeholder="enter your email..." required autocomplete="email">
        <div style="position:absolute;left:-5000px" aria-hidden="true">
          <input type="text" name="{{ site.mailchimp.honeypot }}" tabindex="-1" value="">
        </div>
        <button type="submit" class="sub-popup-btn">
          Get Weekly Jobs &nbsp;→
        </button>
      </form>

      <p class="sub-popup-proof">🎯 8,000+ job seekers already subscribed. Just saying.</p>
    </div>
  </div>
</div>

<script>
(function() {
  var DISMISS_KEY = "sub_popup_dismissed_v2";
  var popup = document.getElementById("sub-popup");
  var closeBtn = document.getElementById("sub-popup-close");
  var form = document.getElementById("sub-popup-form");

  if (!popup || localStorage.getItem(DISMISS_KEY) === "1") return;

  function show() {
    popup.classList.add("popup-visible");
    popup.classList.remove("popup-hidden");
  }

  function hide() {
    popup.classList.remove("popup-visible");
    popup.classList.add("popup-hidden");
    localStorage.setItem(DISMISS_KEY, "1");
  }

  // Slide up from bottom-right after 3 seconds
  setTimeout(show, 3000);

  closeBtn && closeBtn.addEventListener("click", hide);

  form && form.addEventListener("submit", function() {
    setTimeout(hide, 800);
  });

  // Escape key dismiss
  document.addEventListener("keydown", function(e) {
    if (e.key === "Escape" && popup.classList.contains("popup-visible")) hide();
  });
})();
</script>
